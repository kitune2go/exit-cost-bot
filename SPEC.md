# EXIT COST 仕様書

- 版: 1.0
- 対象実装: `exit-cost-bot` v0.1.0
- 最終更新: 2026-09-15
- 対象読者: 本リポジトリの開発者、レビュアー、自己ホスト運用者

本書は EXIT COST の**現在の実装仕様**を確定させたものである。未実装の要求は「§13 未実装・既知の制約」に分離して記載し、実装済み仕様と混在させない。

---

## 1. 目的とコンセプト

EXIT COST は、入力された対象（組織、制度、市場、企業、宗教団体、コミュニティ、人間関係、プラットフォーム、政策、言説など）を、**参加時の誘因と退出時の制約の非対称性**から分析する小規模な LLM アプリケーションである。

中心命題:

> 支配とは、入口を閉じることではなく、出口を閉じることで成立する。

ただし本システムは断罪器ではない。通常のスイッチングコスト、制度的ロックイン、意図的な出口封鎖を**区別**し、反証条件を提示したうえで、分析全体を一文の「圧縮命題」に圧縮することを目的とする。

### 1.1 設計原則（REQ-P）

| ID | 原則 |
|----|------|
| REQ-P1 | 入口で提示される誘因を特定する |
| REQ-P2 | 参加後に形成される依存を特定する |
| REQ-P3 | 退出時に発生するコストを特定する |
| REQ-P4 | 「自由に参加できること」と「自由に退出できること」を分離する |
| REQ-P5 | 意図的支配と制度的帰結を区別し、悪意を推定しない |
| REQ-P6 | 反証可能性を保ち、出口が開いているならそう明言する |
| REQ-P7 | 分析全体を一文へ圧縮する |

これらは `src/prompt.js` のシステムプロンプトとして実装される。プロンプトを変更する場合、本表との整合を維持すること。

## 2. スコープ

### 2.1 スコープ内

- 単一テキスト入力に対する 1 回完結の構造分析
- ブラウザ UI（単一ページ、素の HTML/CSS/ES モジュール）
- HTTP API（JSON）
- OpenAI API および OpenAI 互換エンドポイント（vLLM / Ollama 等）への接続
- Basic 認証とプロセス内レート制限による最低限の公開保護

### 2.2 スコープ外（非目標）

- 会話履歴・マルチターン対話
- 分析結果の永続化、ユーザーアカウント、課金
- 外部データ取得（Web 検索、クローリング、RAG）
- 複数プロセス／水平スケールを前提とした共有状態
- 特定の個人・団体に対する事実認定や法的判断の提供

## 3. 用語定義

| 用語 | 定義 |
|------|------|
| 入口（Entry） | 参加時に提示される誘因。例: 無料、承認、所属感、将来性 |
| 依存（Dependency） | 参加後に形成される、離脱を困難にする結びつき。例: 収入、人間関係、データ、資格 |
| 出口コスト（Exit Cost） | 退出時に発生する金銭的・社会的・心理的コスト |
| 圧縮命題（Compressed Thesis） | 分析全体を一般化した一文 |
| 事例（Case） | ユーザーが入力する分析対象テキスト |

## 4. 判定区分（REQ-J）

`### 判定` セクションは以下の 4 区分のいずれかを中心に述べる。必要なら留保を付す。

| 区分 | 定義 |
|------|------|
| 通常のスイッチングコスト | 乗り換えに手間や費用は伴うが、退出の自由は実質的に保たれている |
| 制度的ロックイン | 設計の意図とは独立に、制度・構造の帰結として退出が困難になっている |
| 出口封鎖の疑い | 退出を困難にする仕組みが意図的に設けられている疑いがある |
| 情報不足 | 入力情報だけでは上記の区別ができない |

REQ-J1: 個人・集団を犯罪者、カルト、反社会的勢力等と断定するには、入力テキストのみでは根拠として不十分であると扱う。

## 5. 出力仕様（REQ-O）

モデル出力は Markdown で、以下の 7 セクションをこの順に含む。

| 順 | 見出し | 内容 |
|----|--------|------|
| 1 | `### 判定` | §4 の区分 |
| 2 | `### 入口` | 入口で与えられる誘因 |
| 3 | `### 依存` | 参加後に形成される依存関係 |
| 4 | `### 出口` | 退出時のコストと制約 |
| 5 | `### 意図と構造` | 意図的支配／制度的帰結／判断不能の切り分け |
| 6 | `### 反証` | 分析が誤りになる条件、出口が開いている事実 |
| 7 | `### 圧縮命題` | 一文のみ |

- REQ-O1: 見出しレベルは `###`（h3）で固定する。
- REQ-O2: `### 圧縮命題` は一文のみとし、事例ごとに新規に生成する（定型文の再利用を禁ずる）。
- REQ-O3: サーバは出力を検証・整形せず、モデル応答をそのまま返す。書式の担保はプロンプト側の責務である。
- REQ-O4: UI は結果を `<pre>` にプレーンテキストとして表示する。Markdown レンダリングは行わない（§12）。

## 6. システム構成

```
ブラウザ (public/index.html, app.js, style.css)
    │  POST /api/analyze  {text}
    ▼
Express サーバ (src/server.js)
    │  basicAuth → express.static / rateLimit
    │  SYSTEM_PROMPT (src/prompt.js)
    ▼
LLM プロバイダ
    ├─ OpenAI Responses API            (既定)
    └─ OpenAI 互換 Chat Completions    (LLM_BASE_URL 設定時)
```

- ランタイム: Node.js >= 22（`process.loadEnvFile` と `node --watch` に依存）
- 依存パッケージ: `express` ^5.2.1、`openai` ^7.15.0
- モジュール形式: ESM (`"type": "module"`)
- 状態: プロセスメモリ上のレート制限バケットのみ。DB・ファイル永続化なし。

### 6.1 起動

| コマンド | 用途 |
|----------|------|
| `npm run dev` | `node --watch src/server.js`（開発） |
| `npm start` | `node src/server.js`（本番） |
| `npm run check` | `node --check src/server.js`（構文チェック） |

起動時に `process.loadEnvFile()` でカレントディレクトリの `.env` を読み込む。`.env` が存在しない場合（`ENOENT`）は無視し、それ以外の読み込みエラーは起動を失敗させる。

サーバは `0.0.0.0:${PORT}` で待ち受ける。`REQUIRE_AUTH` が `true` でない場合、起動ログに警告を出力する。

## 7. HTTP API 仕様

共通:
- リクエストボディは JSON、サイズ上限 32 KB（超過時は Express が 413 を返す）。
- `trust proxy` は `1`（リバースプロキシ 1 段を信頼し、`X-Forwarded-For` の最終ホップをクライアント IP とする）。
- CORS ヘッダは付与しない。したがってブラウザからのクロスオリジン呼び出しは成立しない（同一オリジン前提）。

### 7.1 `GET /health`

- 認証: **不要**（認証ミドルウェアより前に登録される）
- 目的: デプロイ先の死活監視
- レスポンス 200:

```json
{
  "ok": true,
  "provider": "openai",
  "model": "gpt-5.6-luna"
}
```

| フィールド | 型 | 説明 |
|------------|----|------|
| `ok` | boolean | 常に `true` |
| `provider` | string | `LLM_BASE_URL` が設定されていれば `"openai-compatible"`、なければ `"openai"` |
| `model` | string | 解決済みモデル名 |

REQ-H1: 本エンドポイントは API キー、パスワード、接続先 URL 等の秘密情報を返してはならない。

### 7.2 `POST /api/analyze`

- 認証: `REQUIRE_AUTH=true` のとき必要
- レート制限: 適用あり（§9）
- リクエスト:

```json
{ "text": "分析対象の事例テキスト" }
```

| フィールド | 型 | 必須 | 制約 |
|------------|----|------|------|
| `text` | string | 必須 | trim 後に 1 文字以上、12,000 文字以下 |

- レスポンス 200:

```json
{ "analysis": "### 判定\n…\n### 圧縮命題\n…" }
```

モデル応答が空文字だった場合、`analysis` は `"出力を取得できませんでした。"` となる（200 のまま）。

- エラーレスポンス: `{ "error": "<日本語メッセージ>" }`

| ステータス | 条件 | メッセージ |
|------------|------|------------|
| 400 | `text` が未指定・非文字列・空 | 分析する文章を入力してください。 |
| 400 | `text` が 12,000 文字超 | 入力が長すぎます。12,000文字以内にしてください。 |
| 401 | Basic 認証失敗（§8） | Authentication required. / Invalid credentials.（`text/plain`） |
| 401 | LLM 側の認証失敗 | LLM側の認証情報を確認してください。 |
| 413 | ボディが 32 KB 超 | Express 既定のエラー |
| 429 | レート制限超過 | 短時間の利用上限に達しました。しばらくしてから再実行してください。 |
| 500 | `OPENAI_API_KEY` と `LLM_BASE_URL` の双方が未設定 | OPENAI_API_KEY または LLM_BASE_URL が設定されていません。 |
| 503 | `REQUIRE_AUTH=true` かつ `APP_PASSWORD` 未設定 | APP_PASSWORD is not configured.（`text/plain`） |
| upstream status / 500 | その他の LLM 呼び出し失敗 | 分析に失敗しました。接続先とモデル設定を確認してください。 |

REQ-A1: LLM 呼び出しの失敗時、上流エラーのステータスコードを転写し、本文には上流の生メッセージを含めない（内部情報の漏洩防止）。詳細は `console.error` にのみ出力する。

### 7.3 静的ファイル配信

`express.static("public")` を認証ミドルウェアの**後**に登録する。したがって `REQUIRE_AUTH=true` のとき、`/`、`/app.js`、`/style.css` を含む UI 全体が認証対象となる。

## 8. 認証仕様

- 方式: HTTP Basic 認証（`WWW-Authenticate: Basic realm="EXIT COST"`）
- 適用範囲: `/health` を除く全ルート
- 有効化条件: `REQUIRE_AUTH` が文字列 `"true"` と厳密一致する場合のみ（`1`、`TRUE` 等は無効）

| ID | 要件 |
|----|------|
| REQ-S1 | `REQUIRE_AUTH=true` かつ `APP_PASSWORD` が空のとき、保護対象は 503 を返す（無防備な公開を起こさない） |
| REQ-S2 | ユーザ名・パスワードの比較は `crypto.timingSafeEqual` で行う。長さが異なる場合は比較前に不一致とする |
| REQ-S3 | Base64 デコードに失敗した資格情報は空文字として扱い、認証失敗にする |
| REQ-S4 | `APP_USERNAME` の既定値は `exit`、`APP_PASSWORD` に既定値はない |

認証はレート制限より前段に置かれる。すなわち認証失敗リクエストはレート制限のカウント対象にならない。

## 9. レート制限仕様

- 適用範囲: `POST /api/analyze` のみ
- キー: `req.ip`（取得できない場合は `"unknown"`）
- アルゴリズム: 固定ウィンドウ方式。`Map` にキーごとの `{count, resetAt}` を保持する。

| ID | 要件 |
|----|------|
| REQ-R1 | 1 ウィンドウあたり `RATE_LIMIT_MAX` 件まで成功させ、`RATE_LIMIT_MAX + 1` 件目を 429 とする |
| REQ-R2 | ウィンドウ長は `RATE_LIMIT_WINDOW_MINUTES` 分。`resetAt` 経過後、そのキーのカウントは 1 にリセットされる |
| REQ-R3 | 429 応答には残り秒数を `Retry-After` ヘッダ（秒、最小 1）で付与する |
| REQ-R4 | 本実装は単一プロセス内でのみ有効であり、複数インスタンス構成では上限が実質的にインスタンス数倍になる |

## 10. LLM プロバイダ仕様

### 10.1 モード選択

`LLM_BASE_URL`（trim 後）が非空なら **OpenAI 互換モード**、そうでなければ **OpenAI モード**。

| | OpenAI モード | OpenAI 互換モード |
|---|---|---|
| 呼び出し | `client.responses.create` | `client.chat.completions.create` |
| プロンプト渡し | `instructions: SYSTEM_PROMPT`、`input: text` | `messages: [system, user]` |
| 出力取得 | `response.output_text` | `response.choices[0].message.content` |
| APIキー | `OPENAI_API_KEY`（必須） | `LLM_API_KEY`（未設定時は `"local"` を送出） |
| baseURL | SDK 既定 | `LLM_BASE_URL` |

REQ-L1: 認証不要のローカル推論サーバ（Ollama 等）に対しては、`LLM_API_KEY` 未設定でもダミー値 `"local"` を送って接続を成立させる。

### 10.2 モデル解決順

```
LLM_MODEL → OPENAI_MODEL → 既定値
```

既定値は、OpenAI 互換モードでは `qwen3:8b`、OpenAI モードでは `gpt-5.6-luna`。

REQ-L2: 生成パラメータ（temperature、max tokens、timeout 等）は指定せず、プロバイダ既定に従う。
REQ-L3: ストリーミングは使用しない。応答は完了後に一括で返す。

## 11. 設定（環境変数）

| 変数 | 必須 | 既定値 | 説明 |
|------|------|--------|------|
| `PORT` | 任意 | `3000` | 待ち受けポート |
| `REQUIRE_AUTH` | 任意 | `false` | `"true"` で Basic 認証を有効化 |
| `APP_USERNAME` | 任意 | `exit` | Basic 認証ユーザ名 |
| `APP_PASSWORD` | 条件付き必須 | なし | `REQUIRE_AUTH=true` のとき必須 |
| `RATE_LIMIT_MAX` | 任意 | `20` | ウィンドウあたり許可リクエスト数 |
| `RATE_LIMIT_WINDOW_MINUTES` | 任意 | `10` | ウィンドウ長（分） |
| `OPENAI_API_KEY` | 条件付き必須 | なし | OpenAI モードで必須 |
| `OPENAI_MODEL` | 任意 | `gpt-5.6-luna` | OpenAI モードのモデル |
| `LLM_BASE_URL` | 任意 | なし | 設定時に OpenAI 互換モードへ切り替わる |
| `LLM_API_KEY` | 任意 | `local` | 互換モードの API キー |
| `LLM_MODEL` | 任意 | `qwen3:8b` | 互換モードのモデル |

REQ-C1: 秘密情報（`APP_PASSWORD`、`OPENAI_API_KEY`、`LLM_API_KEY`）はリポジトリにコミットせず、ホスティング側の Secret／Environment Variable として設定する。`.env` は `.gitignore` 済み。

### 11.1 デプロイ（Render）

`render.yaml` に Web サービス定義を含む。`buildCommand: npm install`、`startCommand: npm start`、`healthCheckPath: /health`。`APP_PASSWORD` と `OPENAI_API_KEY` は `sync: false`（Render 側で個別設定）とし、`REQUIRE_AUTH` は `"true"` を既定とする。

## 12. フロントエンド仕様

単一ページ（`public/index.html`）。ビルド工程なし、ES モジュールを直接読み込む。

| ID | 要件 |
|----|------|
| REQ-U1 | テキストエリアは `maxlength="12000"`。入力ごとに `N / 12000` の文字数を表示する |
| REQ-U2 | 「分析する」押下時、入力が空なら API を呼ばずにメッセージを表示する |
| REQ-U3 | 分析中はボタンを無効化し、ステータスに `構造を分析しています…` を表示する。完了・失敗にかかわらず再度有効化する |
| REQ-U4 | 結果は `<pre>` に `textContent` で挿入する（HTML として解釈させない）。Markdown 記法は生のまま表示される |
| REQ-U5 | エラー時はサーバの `error` フィールドをステータス行に表示する |
| REQ-U6 | 出力領域は `aria-live="polite"` とする |
| REQ-U7 | コピーボタンは結果取得後に有効化し、`navigator.clipboard` で本文をコピーしたのち 1.2 秒間「コピー済み」を表示する |
| REQ-U8 | 表示はダークテーマ固定（`color-scheme: dark`）。600px 以下で余白とテキストエリア高を縮小する |

## 13. 未実装・既知の制約

本書の版において**未解決**であり、実装は現状のままである項目。修正時は本節と該当章を同時に更新すること。

| ID | 内容 | 影響 |
|----|------|------|
| GAP-01 | モデル解決ロジックが `/health` と `/api/analyze` で重複し、既定値が食い違う。`LLM_BASE_URL` のみ設定し `LLM_MODEL`/`OPENAI_MODEL` を省略した場合、`/health` は `gpt-5.6-luna` を報告するが実際の呼び出しは `qwen3:8b` を使う | 監視情報の不正確さ |
| GAP-02 | レート制限の `Map` に期限切れバケットの掃除がなく、IP 数に比例してメモリが増加し続ける | 長期稼働時のメモリ増加 |
| GAP-03 | レート制限がプロセス内状態のため、複数インスタンスでは上限が守られない（README 記載のとおり Redis 等の共有ストアが必要） | 水平スケール時の実効上限のずれ |
| GAP-04 | LLM 呼び出しにタイムアウト・リトライ・キャンセルがない。上流が応答しない場合リクエストが滞留する | 可用性 |
| GAP-05 | 上流エラーの `status` をそのまま `res.status()` に渡す。HTTP 範囲外の値が来ると例外になり得る | 堅牢性 |
| GAP-06 | 出力の書式検証がない。モデルが 7 セクション構成を崩しても検出されない（REQ-O3） | 出力品質 |
| GAP-07 | 自動テスト・CI が存在しない（`npm run check` は構文チェックのみ） | 回帰検出 |
| GAP-08 | 構造化ログ、リクエスト ID、メトリクスがない | 運用時の追跡 |
| GAP-09 | ストリーミング応答が未対応のため、長文分析では完了まで無表示の待ち時間が生じる | 体感性能 |

## 14. 非機能要件

| ID | 要件 |
|----|------|
| REQ-N1 | サーバ自体の処理は同期的に軽量であり、レスポンス時間の支配項は LLM の応答時間である |
| REQ-N2 | 単一プロセスで動作し、外部データストアを必要としない |
| REQ-N3 | 秘密情報をレスポンス本文・クライアントコード・リポジトリに含めない |
| REQ-N4 | Node.js 22 以上で動作する（`engines.node: ">=22"`） |
| REQ-N5 | 依存パッケージは `express` と `openai` の 2 つに限定し、増やす場合は理由を本書に記録する |

## 15. 受け入れ条件

実装変更時、以下がすべて満たされることを確認する。

- [ ] AC-01 `REQUIRE_AUTH=false` で `npm start` 後、`http://localhost:3000` に UI が表示される
- [ ] AC-02 `GET /health` が認証なしで 200 と `{ok, provider, model}` を返し、秘密情報を含まない
- [ ] AC-03 `REQUIRE_AUTH=true` かつ `APP_PASSWORD` 未設定で、`/` および `/api/analyze` が 503 を返す
- [ ] AC-04 `REQUIRE_AUTH=true` かつ正しい資格情報で UI と API が利用でき、誤った資格情報で 401 になる
- [ ] AC-05 空文字および 12,001 文字の `text` がいずれも 400 になる
- [ ] AC-06 `RATE_LIMIT_MAX` 件の連続成功後、次のリクエストが 429 と `Retry-After` を返す
- [ ] AC-07 キーもベース URL も未設定のとき `/api/analyze` が 500 と設定不足メッセージを返す
- [ ] AC-08 OpenAI モード・OpenAI 互換モードの双方で分析が返る
- [ ] AC-09 返却された分析が §5 の 7 セクションをこの順に含み、`### 圧縮命題` が一文である
- [ ] AC-10 退出が容易な事例を入力したとき、判定が「通常のスイッチングコスト」側に倒れ、`### 反証` に出口が開いている根拠が示される（REQ-P6）

## 16. 変更履歴

| 版 | 日付 | 内容 |
|----|------|------|
| 1.0 | 2026-09-15 | 初版。v0.1.0 の実装仕様を確定し、未解決項目を GAP として分離 |
