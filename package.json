{
  "name": "exit-cost-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Analyze systems through entry incentives, dependency, exit costs, and a compressed thesis.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.2.1",
    "openai": "^7.15.0"
  }
}
